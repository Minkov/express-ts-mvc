extend ../_layout

block content
    form(action="/auth/login" method="POST")
        label
            | Username:
            input(name="username")
        label
            | Password:
            input(name="password" type="password")
        button.btn
            | Login